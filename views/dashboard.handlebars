{{#if posts.length}}
{{#each posts as |post|}}
<div class="post">
  <h2>{{post.title}}</h2>  
  <h3>By: {{user.username}} at {{format_date post.createdAt}}</h3>
  <p>{{post.content}}</p>
</div>
{{/each}}
{{else}}
<div>
  No posts yet
</div>
{{/if}}
