{{#each posts as |post|}}
<h2 class="post">
  <h2>{{post.title}} {{format_date post.createdAt}}</h2>
  <p>{{post.content}}</p>
</div>
{{else}}
<div class="post">
  <h2>No posts yet</h2>
</div>
{{/each}}