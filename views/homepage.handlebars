{{#each posts as |post|}}
<a class="post" href="/posts/{{post.id}}">
  <h2>{{post.title}}</h2>  
  <h3>By: {{user.username}} at {{format_date post.createdAt}}</h3>
  <p>{{post.content}}</p>
</a>
{{else}}
<div class="post">
  <h2>No posts yet</h2>
</div>
{{/each}}